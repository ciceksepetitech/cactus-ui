import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description
} from '@storybook/addon-docs';
import { useState } from 'react';
import '../../styles.css';
import Checkbox from '..';

<Meta title="Components/Checkbox" component={Checkbox} />

## Checkbox

<p
  style={{
    color: 'white',
    padding: '10px',
    fontWeight: 'bold',
    borderRadius: '4px',
    backgroundColor: '#1B5193'
  }}
>
  checkbox components allow a user to input a simple boolean true/false
  selection
</p>

#### Stories

<hr />

##### Default

<Canvas>
  <Story name="Default" inline={false}>
    {(args) => {
      const [checked, setChecked] = useState(true);
      const [cactusChecked, setCactusChecked] = useState(false);
      return (
        <div style={{ display: 'flex', alignItems: 'center' }}>
          <Checkbox
            id="rose"
            name="rose"
            value="rose"
            checked={checked}
            onChange={() => setChecked(!checked)}
          />
          <label style={{ marginRight: '10px' }} htmlFor="rose">
            rose
          </label>
          <Checkbox
            id="cactus"
            name="cactus"
            value="cactus"
            checked={cactusChecked}
            onChange={() => setCactusChecked(!cactusChecked)}
          />
          <label style={{ marginRight: '10px' }} htmlFor="cactus">
            cactus
          </label>
          <Checkbox id="daisy" name="daisy" value="daisy" indeterminate />
          <label style={{ marginRight: '10px' }} htmlFor="daisy">
            daisy
          </label>
          <Checkbox
            id="uncontrolled"
            name="uncontrolled"
            value="uncontrolled"
          />
          <label style={{ marginRight: '10px' }} htmlFor="uncontrolled">
            uncontrolled
          </label>
          <Checkbox
            id="disabled"
            disabled={true}
            name="disabled"
            value="disabled"
          />
          <label style={{ marginRight: '10px' }} htmlFor="disabled">
            disabled
          </label>
          <Checkbox
            defaultChecked
            id="defaultChecked"
            name="defaultChecked"
            value="defaultChecked"
          />
          <label style={{ marginRight: '10px' }} htmlFor="defaultChecked">
            defaultChecked
          </label>
        </div>
      );
    }}
  </Story>
</Canvas>

<Description story="Default">
  Example of a default behavior of Checkbox component
</Description>

<ArgsTable story="Default" />
